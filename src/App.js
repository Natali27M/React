import {Form, Header, Text} from "./components";

const App = () => {
    return (
        <div>
            <Header/>
            <Form/>
            <Text/>
        </div>
    );
};

export default App;

